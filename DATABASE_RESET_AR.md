# تفريغ قاعدة البيانات وإعادة بناء البيانات من الصفر

هذا الدليل يشرح إزاي تمسح **كل** البيانات الموجودة في قاعدة البيانات
(الفنادق، الأسعار، الوجهات، الرحلات، الحجوزات، الفواتير، المحافظ … إلخ)
وترجع تبني بيانات الفنادق وغيرها من جديد، من غير ما تلمس شكل قاعدة
البيانات نفسها (الجداول والفهارس والـ migrations تفضل زي ما هي).

---

## 1. الأمر الأساسي

شغّله من **جوه** خدمة Railway نفسها:

```bash
railway ssh                                   # شِل جوه الخدمة

# معاينة فقط — يعرض عدد الصفوف في كل جدول ولا يمسح أي حاجة
npx ts-node prisma/reset-all-data.ts

# التنفيذ الفعلي — يفرّغ كل الجداول
npx ts-node prisma/reset-all-data.ts --yes
```

### ⚠️ ليه `railway ssh` مش `railway run`

الـ `DATABASE_URL` بتاعكم بيشاور على `postgres.railway.internal` — ده اسم
على **الشبكة الخاصة** بتاعة Railway، مابيتحلّش غير من جوه Railway نفسها.

و`railway run` بيشغّل الأمر على **جهازك إنت** وبس بيحقن متغيرات Railway،
فالاسم ده مش هيتحل عندك والاتصال هيفشل.

يعني عندك طريقتين:

```bash
# ١) من جوه الخدمة — الأسهل والأضمن
railway ssh
npx ts-node prisma/reset-all-data.ts --yes

# ٢) من جهازك — لازم تستخدم اللينك العام بدل الخاص
#    (Postgres → Variables → DATABASE_PUBLIC_URL، هيكون *.proxy.rlwy.net)
DATABASE_URL="$DATABASE_PUBLIC_URL" npx ts-node prisma/reset-all-data.ts --yes
```

أو محليًا لو الـ `DATABASE_URL` موجود في ملف `.env`:

```bash
npm run db:reset:data          # معاينة
npm run db:reset:data -- --yes # تنفيذ
```

> السكربت بيطبع **اسم السيرفر واسم قاعدة البيانات** اللي هيمسحها قبل ما
> يلمس أي حاجة. اتأكد من السطر ده إنه فعلًا القاعدة اللي إنت قاصدها.

> السكربت **معاينة افتراضيًا**. من غير `--yes` مش بيمسح أي حاجة، بس بيطبع
> تقرير بعدد الصفوف في كل جدول عشان تشوف إنت رايح تمسح إيه بالظبط.

المسح بيتم في أمر `TRUNCATE` واحد على كل الجداول مع بعض، يعني إما يتم كله
أو مايتمش خالص — القاعدة عمرها ما هتفضل نص فاضية. بس لأن `TRUNCATE` بياخد
قفل كامل على الجداول، لو التطبيق شغّال وبيقرا من القاعدة في نفس اللحظة
السكربت ممكن يستنى شوية لحد ما الطلبات الشغّالة تخلص. الأفضل تشغّله في
وقت مفيهوش استخدام، أو توقّف الخدمة لدقيقة.

---

## 2. إيه اللي بيتمسح وإيه اللي بيفضل

| الحاجة | بتتمسح؟ | ملاحظات |
|---|---|---|
| كل صفوف الـ 48 جدول | ✅ أيوه | فنادق، أسعار، وجهات، أنشطة، مواصلات، مطارات، حجوزات، فواتير، محافظ، فاوتشرات، قوالب الواجهة … |
| حساب الـ SUPERADMIN | ❌ لأ | بيترجع بنفس الإيميل و**نفس الباسورد** بعد المسح، عشان متتقفلش برّه النظام |
| شكل قاعدة البيانات (الجداول/الفهارس) | ❌ لأ | مفيش `DROP` ولا `CREATE` خالص |
| تاريخ الـ migrations (`_prisma_migrations`) | ❌ لأ | محمي — لو اتمسح، أول `migrate deploy` جاي هيحاول يعيد تنفيذ كل حاجة على قاعدة موجودة أصلًا ويفشل |
| الصور والملفات المرفوعة على الديسك | ❌ لأ افتراضيًا | استخدم `--files` لو عايز تمسحها كمان |

### خيارات إضافية

```bash
# امسح الملفات المرفوعة كمان (صور الفنادق، جوازات السفر، PDF الفواتير)
npx ts-node prisma/reset-all-data.ts --yes --files

# امسح حساب الـ SUPERADMIN هو كمان (لازم تعمل db:seed بعدها عشان تقدر تدخل)
npx ts-node prisma/reset-all-data.ts --yes --wipe-superadmin
```

---

## 3. بعد المسح — مفيش أي أمر تاني لازم يتشغّل

بعد `--yes` القاعدة بتبقى **فاضية**، وده المطلوب لو فريقك هيدخّل الفنادق
والأسعار بإيده من البورتال (Admin → Hotels). **قف هنا.**

الاستثناء الوحيد: لو استخدمت `--wipe-superadmin`، محتاج تعمل حساب دخول:

```bash
npm run db:seed
```

### 🚨 `db:seed:catalog` بيرجّع الفنادق تاني

```bash
npm run db:seed:catalog   # ← بيزرع 209 فندق من ملفات الـ seed
```

لو شغّلته بعد المسح على طول، هتلاقي الفنادق رجعت **وإنت فاكر إن المسح فشل**
— المسح شغال تمام، بس ده بيعيد إنشاءها من الأول. مشغّلهوش غير لما تكون
**فعلًا** عايز الكتالوج الجاهز يرجع.

بيشغّل بالترتيب:

| الأمر | بيعمل إيه |
|---|---|
| `db:seed:hotels` | كتالوج الفنادق (199 فندق) + الوجهات بتاعتهم |
| `db:seed:mea` | وجهات ورحلات وأنشطة وأسعار المواصلات من رate sheet بتاع MEA |
| `db:seed:airports` | المطارات المصرية (CAI, HRG, SSH, LXR, ASW, HBE, MHH) |
| `db:seed:destinations` | صور الوجهات من ويكيبيديا (محتاج إنترنت) |
| `db:apply:images` | صور الفنادق الرئيسية |

ولو عايز تشغّلهم واحد واحد:

```bash
npm run db:seed:hotels
npm run db:seed:mea
npm run db:seed:airports
npm run db:seed:destinations
npm run db:apply:images
```

بعد كده الشركات والمستخدمين بتوعهم بيتعملوا من داخل البورتال بحساب الـ
SUPERADMIN — مفيش seed بيعمل شركات وهمية.

---

## 4. الخطوات كاملة (نسخ ولصق)

**الحالة الشائعة — قاعدة فاضية والفريق يدخّل الداتا بنفسه:**

```bash
# 0. ادخل جوه الخدمة
railway ssh

# 1. شوف إنت رايح تمسح إيه (واتأكد من اسم القاعدة اللي هيطبعه)
npx ts-node prisma/reset-all-data.ts

# 2. امسح — وخلاص، مفيش خطوة تالتة
npx ts-node prisma/reset-all-data.ts --yes
```

**لو عايز الكتالوج الجاهز يرجع بدل ما تدخّله بإيدك**، زوّد بعدها:

```bash
npm run db:seed:catalog
```

---

## 5. ليه مش `prisma migrate reset`

في الـ `package.json` فيه أمر `db:reset` بيشغّل `prisma migrate reset --force`.
**متستخدمهوش على قاعدة بيانات شغّالة** — ده بيعمل `DROP` للـ schema كلها
ويعيد تنفيذ كل الـ migrations من الأول:

- محتاج صلاحيات DDL على قاعدة الإنتاج.
- بيوقّع التطبيق وهو شغّال.
- أي اختلاف يدوي في القاعدة بيضيع.

`reset-all-data.ts` بيمسح **صفوف** بس، فالتطبيق بيفضل شغّال والجداول
والفهارس والـ enums زي ما هي.

---

## 6. ملاحظة على السكربت القديم

كان فيه `prisma/clear-data.ts` بيكتب في الآخر `✓ All data cleared` — بس هو
كان بيمسح 19 جدول بالاسم واحد واحد، والـ schema فيها 48 جدول دلوقتي.
يعني كان بيسيب وراه — من غير ما يقول — الوجهات، أسعار الفنادق
(`HotelRate`)، طلبات عروض الأسعار، الفاوتشرات، أسعار المواصلات،
المطارات، باقات الأنشطة، العروض، باقات الـ SIM، الفواتير المجمّعة،
أسعار الأسواق، وقوالب الواجهة.

اتشال واتبدل بـ `reset-all-data.ts` اللي بيسأل قاعدة البيانات نفسها عن
جداولها، فأي جدول جديد يتضاف للـ schema بكرة هيتغطى تلقائيًا من غير أي
تعديل في السكربت.
